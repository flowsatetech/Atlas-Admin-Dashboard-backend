const TREND_DIRECTION = Object.freeze({
    UP: "up",
    DOWN: "down",
    FLAT: "flat"
});

const TREND_DIRECTION_VALUES = Object.freeze(Object.values(TREND_DIRECTION));

module.exports = {
    TREND_DIRECTION,
    TREND_DIRECTION_VALUES
};
